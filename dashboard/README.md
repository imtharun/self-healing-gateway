# Gateway dashboard

The Vite/React frontend provides three views:

- `/` — portfolio landing page.
- `/dashboard` — safe seeded incident demonstration with no backend access.
- `/operator/login` and `/operator` — authenticated live gateway operations.

For setup, architecture, deployment, and verification instructions, see the
repository root `README.md`.
