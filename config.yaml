gateway:
  port: 8000

routes:
  - path: /api/payments
    upstream: http://localhost:9001
    health_check: /health

  - path: /api/orders
    upstream: http://localhost:9002
    health_check: /health
